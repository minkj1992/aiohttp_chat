async def db_handler(app, handler):
    async def middleware(request):
        pass

    return middleware()


async def authorize(app, handler):
    async def middleware(request):
        pass

    return middleware
